(()=>{
'use strict';
const path=location.pathname;if(!path.startsWith('/calculators/')||path.includes('weight-balance-builder'))return;
const qs=(s,r=document)=>r.querySelector(s);const load=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}};const save=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));return true}catch{return false}};const toast=m=>window.toast?.(m);
function activeAircraft(){const profiles=load('pd-aircraft',[]),id=localStorage.getItem('pd-aircraft-active');return profiles.find(p=>p.id===id)||null}
function prefill(){const p=activeAircraft();if(!p)return;const map={burn:p.fuelBurn,va:p.va,maxWeight:p.maxWeight,psi:p.tirePsi,ppg:p.fuelPpg};for(const [id,v] of Object.entries(map)){const e=document.getElementById(id);if(e&&v!==''&&v!=null){e.value=v;e.dispatchEvent(new Event('input',{bubbles:true}))}}}
function favorite(){const h=qs('.calc-hero h1');if(!h)return;const title=h.textContent.trim(),href=path.endsWith('/')?path:path+'/',key='pd-favorites';let favs=load(key,[]),on=favs.some(x=>x.path===href);const b=document.createElement('button');b.className='pd-star'+(on?' on':'');b.type='button';b.setAttribute('aria-pressed',String(on));b.textContent=on?'★ Favorited':'☆ Favorite';h.insertAdjacentElement('afterend',b);b.addEventListener('click',()=>{favs=load(key,[]);if(favs.some(x=>x.path===href)){favs=favs.filter(x=>x.path!==href);on=false}else{favs=[{title,path:href},...favs.filter(x=>x.path!==href)].slice(0,12);on=true}save(key,favs);b.classList.toggle('on',on);b.setAttribute('aria-pressed',String(on));b.textContent=on?'★ Favorited':'☆ Favorite';toast(on?'Added to favorites':'Removed from favorites')})}
function usage(){const key='pd-usage',u=load(key,{});u[path]=Math.min(999999,(u[path]||0)+1);save(key,u)}
function style(){if(document.getElementById('pdCalcFeatureCss'))return;const s=document.createElement('style');s.id='pdCalcFeatureCss';s.textContent='.pd-star{margin:9px 0 0;border:1px solid var(--line-hi);background:var(--panel);color:var(--muted);border-radius:7px;padding:7px 10px;font:inherit;font-size:10px;cursor:pointer}.pd-star:hover,.pd-star.on{color:var(--text);border-color:var(--silver2)}.calc-actions a{display:inline-flex;align-items:center;border:1px solid #30333a;background:#0c0e10;color:#989ca3;padding:8px 10px;border-radius:7px;font-size:10px}.calc-actions a:hover{color:#fff;border-color:#5b5f66}';document.head.appendChild(s)}
function run(){style();prefill();favorite();usage()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();
})();
