import { chromium } from 'playwright';
const base=(process.env.PILOTDESK_BASE_URL||'https://www.pilot-desk.com').replace(/\/$/,'');
const browser=await chromium.launch({headless:true});
const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true});
const failures=[];const check=async(name,fn)=>{try{await fn();console.log('PASS',name)}catch(e){failures.push(`${name}: ${e.message}`);console.error('FAIL',name,e.message)}};
await check('homepage loads, universal search and mobile navigation appear',async()=>{const r=await page.goto(base+'/',{waitUntil:'domcontentloaded',timeout:30000});if(!r?.ok())throw new Error(`HTTP ${r?.status()}`);await page.waitForSelector('.brand',{timeout:10000});await page.waitForSelector('#pdStartHere',{timeout:10000});await page.waitForSelector('#pdUniversalSearch',{timeout:10000});await page.waitForSelector('[data-pd-global-search]',{timeout:10000});if(!(await page.locator('a[href="/weather.html"]').count()))throw new Error('Weather link missing')});
await check('global search finds flight-training content',async()=>{await page.locator('[data-pd-global-search]').click();await page.waitForSelector('#pdSearchDialog[open]');const input=page.locator('#pdSearchDialog input');await input.fill('private pilot');await page.waitForTimeout(250);if(!(await page.locator('#pdSearchDialog a[href="/training/private-pilot.html"]').count()))throw new Error('Private pilot result missing');await page.locator('#pdSearchDialog button[aria-label="Close search"]').click()});
await check('shared crosswind link prefills, calculates, and has one set of share controls',async()=>{const r=await page.goto(base+'/calculators/crosswind/?runway=350&windDir=320&windSpeed=18',{waitUntil:'domcontentloaded',timeout:30000});if(!r?.ok())throw new Error(`HTTP ${r?.status()}`);await page.waitForSelector('#windSpeed');await page.waitForTimeout(800);if(await page.locator('#runway').inputValue()!=='350')throw new Error('runway query value not applied');if(await page.locator('#windDir').inputValue()!=='320')throw new Error('wind query value not applied');await page.locator('[data-calculate]').click();await page.waitForTimeout(200);const out=(await page.locator('#out0').textContent())?.trim();if(!out||out==='—'||!out.includes('kt'))throw new Error('crosswind result missing');if(await page.locator('[data-pd-copy-link]').count()!==1)throw new Error('duplicate or missing copy-link control');if(await page.locator('[data-pd-share-card]').count()!==1)throw new Error('share-card control missing')});
await check('weather lookup reaches PilotDesk weather API',async()=>{await page.goto(base+'/weather.html',{waitUntil:'domcontentloaded',timeout:30000});await page.locator('#station').fill('KGFK');const responsePromise=page.waitForResponse(r=>r.url().includes('/api/weather?station=KGFK'),{timeout:15000});await page.locator('#weatherForm button[type="submit"]').click();const response=await responsePromise;const body=await response.text();if(!response.ok())throw new Error(`weather API HTTP ${response.status()}: ${body.slice(0,300)}`);await page.waitForFunction(()=>{const status=document.querySelector('#weatherStatus')?.textContent||'',output=document.querySelector('#weatherOutput')?.textContent||'';return !/Fetching|Loading/i.test(status+output)&&/KGFK|METAR|No current products/i.test(status+output)},{timeout:10000});const text=(await page.locator('#weatherStatus').innerText())+' '+(await page.locator('#weatherOutput').innerText());if(/Weather retrieval failed|Weather could not be loaded/i.test(text))throw new Error(text.slice(0,400));if(!/KGFK|METAR/i.test(text))throw new Error('weather response did not render')});
await check('aircraft profile saves locally',async()=>{await page.goto(base+'/aircraft.html',{waitUntil:'domcontentloaded',timeout:30000});await page.evaluate(()=>{localStorage.removeItem('pd-aircraft');localStorage.removeItem('pd-aircraft-active')});await page.reload({waitUntil:'domcontentloaded'});await page.locator('[name="name"]').fill('PilotDesk E2E Test');await page.locator('[name="type"]').fill('TEST');await page.locator('[name="fuelBurn"]').fill('10');await page.locator('#aircraftForm button[type="submit"]').click();await page.waitForTimeout(250);if(!/PilotDesk E2E Test/.test(await page.locator('#aircraftList').innerText()))throw new Error('saved profile not rendered')});
await check('weight and balance builder renders and calculates',async()=>{const r=await page.goto(base+'/calculators/weight-balance-builder/',{waitUntil:'domcontentloaded',timeout:30000});if(!r?.ok())throw new Error(`HTTP ${r?.status()}`);await page.waitForSelector('#wbRows');await page.waitForTimeout(500);if(!(await page.locator('.wb-data').count()))throw new Error('no W&B rows rendered');const total=(await page.locator('#wbW').textContent())?.trim();if(!total||total==='—')throw new Error('W&B total did not calculate')});
await check('every canonical sitemap URL is live and structurally usable',async()=>{const mapResponse=await page.request.get(base+'/sitemap.xml',{timeout:30000});if(!mapResponse.ok())throw new Error(`sitemap returned ${mapResponse.status()}`);const xml=await mapResponse.text();const urls=[...xml.matchAll(/<loc>(https:\/\/www\.pilot-desk\.com[^<]+)<\/loc>/g)].map(m=>m[1].replaceAll('&amp;','&'));if(urls.length<160)throw new Error(`only ${urls.length} canonical URLs found`);if(new Set(urls).size!==urls.length)throw new Error('duplicate canonical URLs in sitemap');if(urls.some(u=>u.includes('pilot-seo-priority')))throw new Error('retired SEO-named guide remains in sitemap');if(!urls.includes(base+'/guides/popular-aviation-tools.html'))throw new Error('popular aviation tools page missing from sitemap');const issues=[];for(let i=0;i<urls.length;i+=12){const batch=urls.slice(i,i+12);await Promise.all(batch.map(async url=>{try{const r=await page.request.get(url,{timeout:30000});if(!r.ok()){issues.push(`${url} HTTP ${r.status()}`);return}const body=await r.text();if(body.length<80){issues.push(`${url} returned an unexpectedly small body`);return}const type=(r.headers()['content-type']||'').toLowerCase();if(type.includes('text/html')){if(!/<title>[^<]+<\/title>/i.test(body))issues.push(`${url} missing title`);if(!/<h1(?:\s|>)/i.test(body))issues.push(`${url} missing H1`);const canonical=(body.match(/<link[^>]+rel=["']canonical["'][^>]+href=["']([^"']+)["']/i)||body.match(/<link[^>]+href=["']([^"']+)["'][^>]+rel=["']canonical["']/i)||[])[1];if(!canonical||!canonical.startsWith(base+'/'))issues.push(`${url} missing valid PilotDesk canonical`)}}catch(e){issues.push(`${url} ${e.message}`)}}))}if(issues.length)throw new Error(`${issues.length} crawl issue(s): ${issues.slice(0,12).join(' | ')}`);console.log(`Checked ${urls.length} canonical production URLs.`)});
await check('training, long-tail guides, sources, privacy and sitemaps are live',async()=>{for(const p of ['/flight-training.html','/training/private-pilot.html','/training/instrument-rating.html','/training/commercial-pilot.html','/training/multiengine.html','/training/cfi.html','/guides/crosswind-component-chart.html','/guides/avgas-weight-per-gallon.html','/guides/three-degree-descent-rate-chart.html','/guides/cessna-172-glide-distance.html','/guides/seminole-vmc-study.html','/guides/popular-aviation-tools.html','/sources.html','/legal/privacy.html','/sitemap.xml','/sitemap-growth.xml','/sitemap-retention.xml']){const r=await page.request.get(base+p,{timeout:30000});if(!r.ok())throw new Error(`${p} returned ${r.status()}`)}});
await check('retired SEO-named guide permanently redirects to the public guide',async()=>{const r=await page.request.get(base+'/guides/pilot-seo-priority.html',{timeout:30000,maxRedirects:0});if(![301,308].includes(r.status()))throw new Error(`expected permanent redirect, got HTTP ${r.status()}`);const location=r.headers()['location']||'';if(!location.includes('/guides/popular-aviation-tools.html'))throw new Error(`unexpected redirect location: ${location||'(missing)'}`)});
await check('popular aviation tools page has a self canonical',async()=>{const r=await page.goto(base+'/guides/popular-aviation-tools.html',{waitUntil:'domcontentloaded',timeout:30000});if(!r?.ok())throw new Error(`HTTP ${r?.status()}`);const canonical=await page.locator('link[rel="canonical"]').getAttribute('href');if(canonical!=='https://www.pilot-desk.com/guides/popular-aviation-tools.html')throw new Error(`unexpected canonical: ${canonical}`);if((await page.locator('h1').innerText()).trim()!=='Popular Aviation Calculators & Pilot References')throw new Error('unexpected H1')});
await browser.close();if(failures.length){console.error(`\n${failures.length} live E2E failure(s):`);failures.forEach(x=>console.error(' - '+x));process.exit(1)}console.log('\nPilotDesk live production E2E checks passed.');
