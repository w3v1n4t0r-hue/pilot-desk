(()=>{
'use strict';
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
function dateValue(v){if(v==null||v==='')return null;const d=new Date((typeof v==='number'&&v<1e12)?v*1000:v);return Number.isFinite(d.getTime())?d:null}
function when(v){const d=dateValue(v);return d?d.toLocaleString(undefined,{year:'numeric',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'}):(v==null||v===''?null:String(v))}
function ageMinutes(v){const d=dateValue(v);if(!d)return null;return Math.max(0,(Date.now()-d.getTime())/60000)}
function loadFavs(){try{const a=JSON.parse(localStorage.getItem(FAV_KEY)||'[]');return Array.isArray(a)?a:[]}catch{return[]}}
function saveFavs(a){try{localStorage.setItem(FAV_KEY,JSON.stringify(a.slice(0,20)))}catch{}}
function renderFavs(){
 const host=$('#wxFavoritesList');if(!host)return;const a=loadFavs();
 host.innerHTML=a.length?a.map(x=>`<a class="wx-favorite" href="/weather.html?station=${encodeURIComponent(x.id)}"><b>${esc(x.id)}</b><span>${esc(x.name||'Open weather')}</span></a>`).join(''):'<a class="wx-favorite" href="/airport.html"><b>ADD AIRPORT</b><span>Save fields you check often</span></a>';
}
function toggleFav(id,name){
 const a=loadFavs(),exists=a.some(x=>x.id===id),next=exists?a.filter(x=>x.id!==id):[{id,name,at:Date.now()},...a.filter(x=>x.id!==id)];
 saveFavs(next);renderFavs();const b=$('#wxFavoriteCurrent');if(b)b.textContent=exists?'Save airport':'Saved';
}
function runways(ap){const out=[];const add=(id,len)=>{String(id||'').toUpperCase().split(/[\/-]/).forEach(x=>{const m=x.match(/^(\d{1,2})([LCR])?$/);if(!m)return;const n=Number(m[1]);if(n<1||n>36)return;const key=m[1].padStart(2,'0')+(m[2]||'');if(!out.some(r=>r.id===key))out.push({id:key,heading:n===36?360:n*10,length:num(len)})})};const visit=(o,d=0)=>{if(!o||typeof o!=='object'||d>4)return;if(Array.isArray(o)){o.forEach(v=>visit(v,d+1));return}for(const [k,v] of Object.entries(o)){if(v&&typeof v==='object')visit(v,d+1);if(typeof v==='string'&&/(rwy|runway|ident)/i.test(k))add(v,o.length??o.len??o.rwyLen??o.runwayLength)}};visit(ap);return out.slice(0,20).sort((a,b)=>a.heading-b.heading)}
function comp(r,wd,ws,gust){const rel=(wd-r.heading)*Math.PI/180,calc=s=>({x:s*Math.sin(rel),h:s*Math.cos(rel)}),a=calc(ws),g=gust==null?null:calc(gust),side=x=>Math.abs(x)<.05?'':x>0?'from right':'from left';return `<article class="wx-runway"><div><b>RWY ${esc(r.id)}</b><small>${r.length?`${Math.round(r.length).toLocaleString()} ft · `:''}approx ${String(r.heading).padStart(3,'0')}° magnetic</small></div><div><small>Head/tail</small><b>${Math.abs(a.h).toFixed(0)} kt ${a.h>=0?'headwind':'tailwind'}</b></div><div><small>Crosswind</small><b>${Math.abs(a.x).toFixed(0)} kt ${esc(side(a.x))}</b></div>${g?`<div><small>Gust xwind</small><b>${Math.abs(g.x).toFixed(0)} kt ${esc(side(g.x))}</b></div>`:''}</article>`}
function compData(r,wd,ws){const rel=(wd-r.heading)*Math.PI/180;return{x:ws*Math.sin(rel),h:ws*Math.cos(rel)}}
function runwayScreen(rw,wd,ws){if(wd==null||ws==null||!rw.length)return null;return rw.map(r=>({r,...compData(r,wd,ws)})).sort((a,b)=>(Math.abs(a.x)+(a.h<0?50:0))-(Math.abs(b.x)+(b.h<0?50:0)))[0]||null}
function renderFlightStops(){
 const host=$('#pdWeatherFlightStops');if(!host)return;const params=new URLSearchParams(location.search),id=params.get('flight')||localStorage.getItem('pd-active-flight');if(!id){host.hidden=true;host.innerHTML='';return}
 try{const all=JSON.parse(localStorage.getItem('pd-saved-flights')||'[]'),f=Array.isArray(all)?all.find(x=>x.id===id):null,parts=String(f?.route||'').trim().split(/\s+/).filter(Boolean);if(!f||parts.length<2){host.hidden=true;return}host.hidden=false;host.innerHTML='<div><small>ACTIVE FLIGHT WEATHER</small><b>'+esc(f.name||parts.join(' → '))+'</b></div><div class="pd-weather-stop-links">'+parts.slice(0,10).map((p,i)=>'<a href="/weather.html?station='+encodeURIComponent(p)+'&flight='+encodeURIComponent(f.id)+'"'+(String(params.get('station')||'').toUpperCase()===p.toUpperCase()?' aria-current="page"':'')+'><span>'+(i===0?'DEP':i===parts.length-1?'DEST':'ENR')+'</span><b>'+esc(p)+'</b></a>').join('')+'</div>'}catch{host.hidden=true}
}
function status(text,state=''){const e=$('#weatherStatus');if(e){e.textContent=text;e.dataset.state=state}}
function shortSource(s){return /backup|NOAA\/NWS/i.test(s)?'AWC + NOAA/NWS':/Aviation Weather Center/i.test(s)?'AWC':s||'Live source'}
async function lookup(id){const form=$('#weatherForm'),out=$('#weatherOutput'),btn=form.querySelector('button[type=submit]');if(!navigator.onLine){status('Offline — live weather unavailable','error');out.innerHTML='<div class="pd-empty">Reconnect to retrieve current aviation weather. Core calculators remain available offline.</div>';return}btn.disabled=true;btn.textContent='Loading…';status('Loading weather…','loading');out.innerHTML='<div class="wx-skeleton">Retrieving METAR, TAF, and airport data…</div>';
try{const r=await fetch('/api/weather?station='+encodeURIComponent(id),{headers:{Accept:'application/json'},cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`Weather service returned ${r.status}`);const station=j.resolvedStation||j.station||id,met=j.metar,taf=j.taf,ap=j.airport||j.stationInfo,source=j.source||'U.S. Aviation Weather Center',sourceShort=shortSource(source);if(!met&&!taf&&!ap){status(`${station}: no current products returned`,'warn');out.innerHTML='<div class="pd-empty">No current METAR, TAF, or station information was returned for this identifier.</div>';return}
const wd=num(met?.wdir),ws=num(met?.wspd),wg=num(met?.wgst),alt=num(met?.altim),elevM=num(met?.elev??ap?.elev),elevFt=elevM==null?null:elevM*3.28084,altInHg=alt==null?null:alt*0.0295299830714,paApprox=elevFt!=null&&altInHg!=null?elevFt+(29.92-altInHg)*1000:null,obsRaw=met?.obsTime??met?.reportTime,obs=when(obsRaw),obsAge=ageMinutes(obsRaw),obsStale=obsAge!=null&&obsAge>90,fetched=when(j.fetchedAt),tafIssueRaw=taf?.issueTime??taf?.bulletinTime,tafTime=when(tafIssueRaw),tafIssueAge=ageMinutes(tafIssueRaw),tafFromRaw=taf?.validTimeFrom??taf?.validFrom??taf?.fcstTimeFrom,tafToRaw=taf?.validTimeTo??taf?.validTo??taf?.fcstTimeTo,tafFrom=dateValue(tafFromRaw),tafTo=dateValue(tafToRaw),tafOlder=tafIssueAge!=null&&tafIssueAge>720,tafExpired=tafTo?Date.now()>tafTo.getTime():false,tafNotStarted=tafFrom?Date.now()<tafFrom.getTime():false,tafWarn=Boolean(taf&&((tafOlder||tafExpired||tafNotStarted))),tafValidity=(tafFrom||tafTo)?`${tafFrom?when(tafFrom):'—'} to ${tafTo?when(tafTo):'—'}`:'',rw=runways(ap),partial=(j.errors||[]).map(e=>e.source).join(','),warnState=obsStale||tafWarn||Boolean(partial);status(`${station} ${obsStale?`observation ${Math.round(obsAge)} min old`:'data received'} · ${sourceShort} · retrieved ${fetched||'now'}${tafExpired?' · TAF validity ended':''}${tafOlder?` · TAF issued ${Math.round(tafIssueAge/60)} hr ago`:''}${j.fallbackUsed?' · backup active':''}${partial?` · partial: ${partial}`:''}`,warnState?'warn':'live');const q=new URLSearchParams();if(wd!=null)q.set('windDir',wd);if(ws!=null)q.set('windSpeed',ws);if(met?.temp!=null)q.set('oat',met.temp);const screen=runwayScreen(rw,wd,ws),crossQ=new URLSearchParams(q);if(screen)crossQ.set('runway',screen.r.heading);const daQ=new URLSearchParams();if(paApprox!=null)daQ.set('pa',Math.round(paApprox));if(met?.temp!=null)daQ.set('oat',met.temp);const flightId=new URLSearchParams(location.search).get('flight')||localStorage.getItem('pd-active-flight')||'',flight=flightId?(()=>{try{const a=JSON.parse(localStorage.getItem('pd-saved-flights')||'[]');return Array.isArray(a)?a.find(x=>x.id===flightId):null}catch{return null}})():null;
const tafWarning=tafWarn?`<div class="safety-strip"><strong>Check TAF currency.</strong> ${tafExpired?'The returned TAF validity period appears to have ended. ':''}${tafNotStarted?'The returned TAF validity period has not started yet. ':''}${tafOlder?`The TAF issue time is about ${Math.round(tafIssueAge/60)} hours old. `:''}Verify the latest forecast at the source before flight planning.</div>`:'';
out.innerHTML=`<section class="wx-summary"><div class="wx-title"><div><span class="badge">${esc(met?.fltCat||sourceShort)}</span><h2>${esc(station)}</h2><p>${esc(ap?.name||met?.name||'Airport weather')}</p></div><div class="pd-actions"><span class="pd-source-chip">${esc(source)}</span><button class="utility-btn" id="wxFavoriteCurrent" type="button">${loadFavs().some(x=>x.id===station)?'Saved':'Save airport'}</button></div></div>${obsStale?`<div class="safety-strip"><strong>Older observation.</strong> This METAR appears to be about ${Math.round(obsAge)} minutes old. Verify current conditions at the source before using it for flight planning.</div>`:''}${tafWarning}<div class="wx-kpis"><div><small>Wind</small><b>${met?(met.wdir==='VRB'?'VRB':`${String(met.wdir??'—').padStart(3,'0')}°`)+' '+(met.wspd??0)+' kt'+(met.wgst?` G${met.wgst}`:''):'—'}</b></div><div><small>Visibility</small><b>${met?.visib!=null?`${esc(met.visib)} SM`:'—'}</b></div><div><small>Altimeter</small><b>${alt!=null?`${(alt*0.0295299830714).toFixed(2)} inHg`:'—'}</b></div><div><small>Temp / dew</small><b>${met?.temp!=null?`${esc(met.temp)}° / ${esc(met.dewp??'—')}°C`:'—'}</b></div></div><p class="fine">${obs?`Observation ${esc(obs)}${obsAge!=null?` · ${Math.round(obsAge)} min old`:''} · `:''}PilotDesk retrieval ${esc(fetched||'—')}</p></section>${wd!=null&&ws!=null&&rw.length?`<section class="wx-block"><div class="pd-card-head"><div><h2>Runway wind components</h2><p>Calculated from the reported wind and runway-number heading approximation.</p></div></div><div class="wx-runways">${rw.map(x=>comp(x,wd,ws,wg)).join('')}</div><p class="fine">Verify current published runway headings and all applicable aircraft, operator, and pilot limits.</p></section>`:''}<section class="wx-raw-grid"><article class="wx-block"><div class="pd-card-head"><h2>METAR</h2><span class="wx-asof">${obs?'AS OF '+esc(obs):'OBS TIME UNAVAILABLE'}</span></div><div class="pd-raw">${esc(met?.rawOb||'No current METAR returned.')}</div></article><article class="wx-block"><div class="pd-card-head"><h2>TAF</h2><span class="wx-asof">${tafTime?'ISSUED '+esc(tafTime):'ISSUE TIME UNAVAILABLE'}</span></div>${tafValidity?`<p class="fine">Validity: ${esc(tafValidity)}</p>`:''}<div class="pd-raw">${esc(taf?.rawTAF||'No current TAF returned.')}</div></article></section><section class="wx-block pd-weather-decisions"><div class="pd-card-head"><div><h2>Carry weather into the plan</h2><p>Turn the observation into the next calculation or planning check without retyping the station data.</p></div></div><div class="pd-weather-decision-grid">
<article><small>RUNWAY WIND</small><h3>${screen?`RWY ${esc(screen.r.id)} orientation screen`:'Runway component setup'}</h3><p>${screen?`Approx. ${Math.abs(screen.x).toFixed(0)} kt crosswind and ${Math.abs(screen.h).toFixed(0)} kt ${screen.h>=0?'headwind':'tailwind'} using runway-number heading.`:'Runway data or steady wind was unavailable.'}</p>${screen?`<a class="pd-btn secondary" data-weather-handoff="crosswind" href="/calculators/crosswind/?${crossQ}">Open crosswind setup →</a>`:''}<small class="pd-weather-caution">METAR winds are true; runway numbers are magnetic approximations. Reconcile references and verify the published runway heading before operational use.</small></article>
<article><small>PERFORMANCE</small><h3>Density-altitude setup</h3><p>${paApprox!=null&&met?.temp!=null?`Approx. pressure altitude ${Math.round(paApprox).toLocaleString()} ft from station elevation + altimeter, with OAT ${esc(met.temp)}°C.`:'Use pressure altitude and OAT from current verified data.'}</p><a class="pd-btn secondary" data-weather-handoff="density-altitude" href="${paApprox!=null&&met?.temp!=null?'/calculators/density-altitude/?'+daQ:'/calculators/pressure-altitude/'}">${paApprox!=null?'Open density altitude':'Find pressure altitude'} →</a><small class="pd-weather-caution">The pressure-altitude handoff is a convenience estimate; use approved aircraft performance data for takeoff, climb, and landing.</small></article>
<article><small>ACTIVE FLIGHT</small><h3>${flight?esc(flight.name||'Saved flight'):'No saved flight attached'}</h3><p>${flight?'This weather lookup is attached to the seven-step flight workflow.':'Start or save a flight to carry weather into loading, math, procedures, and review.'}</p><a class="pd-btn secondary" data-weather-handoff="flight" href="${flight?'/weight-balance.html?flight='+encodeURIComponent(flight.id)+(flight.aircraftId?'&aircraft='+encodeURIComponent(flight.aircraftId):''):'/flights.html?route='+encodeURIComponent(station+' ')}">${flight?'Continue to loading':'Start a saved flight'} →</a></article>
<article><small>VERIFY</small><h3>Airport + official source</h3><p>Check airport details, procedures, report times, and the full official weather picture before the flight.</p><div class="pd-actions"><a class="pd-btn secondary" data-weather-handoff="airport" href="/airport.html?id=${encodeURIComponent(station)}">Airport</a><a class="pd-btn secondary" data-weather-handoff="procedures" href="/procedures.html?ident=${encodeURIComponent(station)}${flight?'&flight='+encodeURIComponent(flight.id):''}">Procedures</a><a class="pd-btn secondary" data-weather-handoff="awc" target="_blank" rel="noopener" href="https://aviationweather.gov/">AWC ↗</a></div></article>
</div></section><p class="fine">For planning only. This is not a complete weather briefing. Verify current conditions and required operational information with the appropriate official sources.</p>`;const nextUrl=new URL(location.href);nextUrl.pathname='/weather.html';nextUrl.search='';nextUrl.searchParams.set('station',station);if(flightId)nextUrl.searchParams.set('flight',flightId);$('#wxFavoriteCurrent')?.addEventListener('click',()=>toggleFav(station,ap?.name||met?.name||station));history.replaceState(null,'',nextUrl.pathname+'?'+nextUrl.searchParams.toString());localStorage.setItem('pd-last-airport',station);document.dispatchEvent(new CustomEvent('pilotdesk:weatherloaded',{detail:{station,stale:obsStale||tafWarn,fallback:Boolean(j.fallbackUsed),hasRunwayContext:Boolean(screen),hasDensityAltitudeSetup:paApprox!=null&&met?.temp!=null}}))}catch(e){console.error(e);status('Weather retrieval failed','error');out.innerHTML=`<div class="wx-error"><h2>Weather could not be loaded</h2><p>${esc(e.message||'Try again in a moment.')}</p><div class="pd-actions"><button class="pd-btn secondary" id="weatherRetry" type="button">Retry</button><a class="pd-btn secondary" target="_blank" rel="noopener" href="https://aviationweather.gov/data/metar/">Open AWC METAR/TAF</a></div></div>`;$('#weatherRetry')?.addEventListener('click',()=>lookup(id))}finally{btn.disabled=false;btn.textContent='Get weather'}}
function submit(e){e.preventDefault();e.stopImmediatePropagation();const id=String(new FormData(e.currentTarget).get('station')||'').trim().toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,4);if(!/^[A-Z0-9]{3,4}$/.test(id))return status('Enter a valid 3- or 4-character station identifier.','error');lookup(id)}
function init(){renderFavs();const f=$('#weatherForm');if(!f)return;f.addEventListener('submit',submit,true);$('#weatherOutput')?.addEventListener('click',e=>{const a=e.target.closest('[data-weather-handoff]');if(a)window.pdTrack?.('Weather Planning Handoff',{action:a.dataset.weatherHandoff||'unknown',hasActiveFlight:(new URLSearchParams(location.search).get('flight')||localStorage.getItem('pd-active-flight'))?'yes':'no'})});renderFlightStops();const params=new URLSearchParams(location.search),flightId=params.get('flight');let flightStation='';if(flightId){try{const flights=JSON.parse(localStorage.getItem('pd-saved-flights')||'[]'),flight=Array.isArray(flights)?flights.find(x=>x.id===flightId):null,parts=String(flight?.route||'').trim().split(/\s+/).filter(Boolean);flightStation=parts.at(-1)||''}catch{}}const id=String(params.get('station')||flightStation||localStorage.getItem('pd-last-airport')||'').toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,4);if(id)f.elements.station.value=id;if(params.get('station')||flightStation)lookup(id);window.addEventListener('online',()=>status('Online — ready for live weather','live'));window.addEventListener('offline',()=>status('Offline — live weather unavailable','error'))}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();
})();
