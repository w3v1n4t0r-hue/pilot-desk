(()=>{
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const numberOrNull=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
  const runwayHeading=id=>{const m=String(id||'').toUpperCase().match(/(?:^|\D)(\d{1,2})(?:[LCR])?(?:$|\D)/);if(!m)return null;let n=Number(m[1]);if(n<1||n>36)return null;return n===36?360:n*10};
  function extractRunways(ap){
    if(!ap||typeof ap!=='object')return [];
    const found=[];const add=(id,length)=>{id=String(id||'').toUpperCase().trim();if(!id)return;id.split(/[\/\-]/).forEach(part=>{const h=runwayHeading(part);if(h!=null&&!found.some(x=>x.id===part.trim()))found.push({id:part.trim(),heading:h,length:numberOrNull(length)})})};
    const visit=(obj,depth=0)=>{if(!obj||typeof obj!=='object'||depth>3)return;if(Array.isArray(obj)){obj.forEach(x=>visit(x,depth+1));return}for(const [k,v] of Object.entries(obj)){const key=k.toLowerCase();if(v&&typeof v==='object')visit(v,depth+1);if(typeof v==='string'&&(key.includes('rwy')||key.includes('runway')||key.includes('ident'))){if(/\d{1,2}[LCR]?/.test(v))add(v,obj.length??obj.len??obj.rwyLen??obj.runwayLength)}if((key==='leident'||key==='heident'||key==='le_ident'||key==='he_ident')&&v)add(v,obj.length??obj.len??obj.rwyLen)}}};visit(ap);return found.slice(0,12).sort((a,b)=>a.heading-b.heading)}
  function componentRow(r,wd,ws,wg){const rel=(wd-r.heading)*Math.PI/180,c=Math.abs(ws*Math.sin(rel)),h=ws*Math.cos(rel),gc=wg!=null?Math.abs(wg*Math.sin(rel)):null,gh=wg!=null?wg*Math.cos(rel):null;const htxt=`${Math.abs(h).toFixed(0)} kt ${h>=0?'headwind':'tailwind'}`;const ctxt=`${c.toFixed(0)} kt crosswind`;const gust=gc==null?'':` · gust: ${Math.abs(gh).toFixed(0)} ${gh>=0?'head':'tail'} / ${gc.toFixed(0)} cross`;return `<div class="pd-card"><div class="pd-card-head"><div><h3>Runway ${esc(r.id)}</h3><p>${r.length?`${Math.round(r.length).toLocaleString()} ft · `:''}approx. heading ${String(Math.round(r.heading)).padStart(3,'0')}°</p></div></div><div class="pd-kv"><div><small>Along runway</small><b>${esc(htxt)}</b></div><div><small>Crosswind</small><b>${esc(ctxt)}</b></div></div>${gust?`<p>${esc(gust.replace(/^ · /,''))}</p>`:''}</div>`}

  async function lookup(form,out,id){
    const btn=form.querySelector('button[type=submit]');if(btn){btn.disabled=true;btn.textContent='Loading…'}out.innerHTML='<p>Loading current aviation weather…</p>';
    try{
      const r=await fetch('/api/weather?station='+encodeURIComponent(id),{headers:{Accept:'application/json'}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Weather lookup failed');const met=d.metar,taf=d.taf,ap=d.airport;if(!met&&!taf&&!ap){out.innerHTML='<div class="pd-card"><h3>No current data found</h3><p>Check the station identifier and try again.</p></div>';return}
      const wd=met?.wdir,windDir=(wd==='VRB'||wd==null)?'VRB':String(wd).padStart(3,'0')+'°',wind=met?`${windDir} at ${met.wspd??0} kt${met.wgst?`, gust ${met.wgst} kt`:''}`:'—';const altHpa=numberOrNull(met?.altim),alt=altHpa==null?'—':(altHpa*0.0295299830714).toFixed(2)+' inHg';const elev=ap?.elev??met?.elev??'—',cat=met?.fltCat?` <span class="pd-flightcat">${esc(met.fltCat)}</span>`:'';
      const q=new URLSearchParams(),wdirNum=numberOrNull(met?.wdir),wspdNum=numberOrNull(met?.wspd),wgstNum=numberOrNull(met?.wgst),oat=numberOrNull(met?.temp);if(wdirNum!=null)q.set('windDir',String(wdirNum));if(wspdNum!=null)q.set('windSpeed',String(wspdNum));if(oat!=null)q.set('oat',String(oat));
      const runways=extractRunways(ap);let runwayHtml='';if(wdirNum!=null&&wspdNum!=null&&runways.length){runwayHtml=`<div class="pd-panel"><h2>Runway wind components</h2><p>Calculated from the current reported wind. Runway headings are approximate; verify published runway data.</p><div class="pd-list">${runways.map(x=>componentRow(x,wdirNum,wspdNum,wgstNum)).join('')}</div></div>`}else if(met){runwayHtml='<div class="pd-panel"><h2>Runway wind components</h2><p>Automatic runway data was not available for this station. Use the prefilled Crosswind calculator below and enter the current runway heading.</p></div>'}
      const partial=d.errors?.length?`<div class="notice">Some supplemental airport data could not be loaded, but the available weather is shown below.</div>`:'';
      out.innerHTML=`<div class="pd-card"><h3>${esc(id)}${cat}</h3><p>${esc(ap?.name||met?.name||'Current station weather')}</p><div class="pd-kv"><div><small>Wind</small><b>${esc(wind)}</b></div><div><small>Visibility</small><b>${esc(met?.visib??'—')} SM</b></div><div><small>Altimeter</small><b>${esc(alt)}</b></div><div><small>Temperature</small><b>${esc(met?.temp??'—')} °C</b></div><div><small>Dew point</small><b>${esc(met?.dewp??'—')} °C</b></div><div><small>Field elevation</small><b>${esc(elev)} ft</b></div></div></div>${partial}${runwayHtml}<div class="pd-panel"><h2>METAR</h2>${met?.rawOb?`<div class="pd-raw">${esc(met.rawOb)}</div>`:'<p>No current METAR returned.</p>'}</div><div class="pd-panel"><h2>TAF</h2>${taf?.rawTAF?`<div class="pd-raw">${esc(taf.rawTAF)}</div>`:'<p>No current TAF returned for this station.</p>'}</div><div class="pd-actions"><a class="pd-btn secondary" href="/calculators/crosswind/?${q}">Crosswind calculator</a><a class="pd-btn secondary" href="/calculators/density-altitude/?${q}">Density altitude</a></div>`;
    }catch(e){console.error(e);out.innerHTML=`<div class="pd-card"><h3>Weather lookup failed</h3><p>${esc(e.message||'Try again in a moment.')}</p></div>`}finally{if(btn){btn.disabled=false;btn.textContent='Get weather'}}
  }
  function handleSubmit(e){const form=e.target;if(!(form instanceof HTMLFormElement)||form.id!=='weatherForm')return;e.preventDefault();e.stopImmediatePropagation();const out=document.getElementById('weatherOutput');if(!out)return;const id=String(form.elements.station?.value||'').trim().toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,4);if(!/^[A-Z0-9]{3,4}$/.test(id)){out.innerHTML='<div class="pd-card"><p>Enter a valid 3- or 4-character station identifier.</p></div>';return}localStorage.setItem('pd-last-airport',id);lookup(form,out,id)}
  document.addEventListener('submit',handleSubmit,true);const init=()=>{const form=document.getElementById('weatherForm');if(!form)return;const last=localStorage.getItem('pd-last-airport');if(last&&!form.elements.station.value)form.elements.station.value=last};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
