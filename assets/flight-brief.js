(()=>{
'use strict';
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const FKEY='pd-saved-flights';
function flights(){try{return JSON.parse(localStorage.getItem(FKEY)||'[]')}catch{return[]}}
function aircraft(){try{return JSON.parse(localStorage.getItem('pd-aircraft')||'[]')}catch{return[]}}
function active(){const a=aircraft(),id=localStorage.getItem('pd-aircraft-active');return a.find(x=>x.id===id)||a[0]||null}
async function json(url,timeout=12000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{headers:{Accept:'application/json'},signal:c.signal}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`Request returned ${r.status}`);return j}finally{clearTimeout(t)}}
function manual(s){const m=String(s).trim().match(/^([^,]+),\s*(-?\d+(?:\.\d+)?),\s*(-?\d+(?:\.\d+)?)$/);if(!m)return null;return{id:m[1].trim().toUpperCase(),lat:Number(m[2]),lon:Number(m[3]),source:'manual'}}
async function resolve(t){const m=manual(t);if(m)return m;return(await json('/api/navdata?ident='+encodeURIComponent(t),10000)).point}
function wxCard(j,label){const m=j?.metar,cat=m?.fltCat||'—';return`<article class="pd-dashboard-block"><div class="pd-airport-title"><div><small>${esc(label)}</small><h3>${esc(j?.resolvedStation||j?.station||'—')}</h3></div><span class="pd-flightcat-pill" data-cat="${esc(cat)}">${esc(cat)}</span></div><div class="pd-raw-report">${esc(m?.rawOb||'No current METAR returned.')}</div><p class="fine">${m?.wdir!=null?`Wind ${esc(m.wdir==='VRB'?'VRB':String(m.wdir).padStart(3,'0')+'°')} ${esc(m.wspd??'—')} kt${m.wgst?` G${esc(m.wgst)}`:''}. `:''}${j?.fallbackUsed?'NOAA/NWS backup active.':''}</p></article>`}
function loadFlight(){const p=new URLSearchParams(location.search),id=p.get('id');if(id){const f=flights().find(x=>x.id===id);if(f)return f}const route=p.get('route');if(route)return{name:'Quick brief',route:route.toUpperCase(),tas:Number(p.get('tas'))||'',burn:Number(p.get('burn'))||'',windDir:Number(p.get('windDir'))||270,windSpeed:Number(p.get('windSpeed'))||20,variation:Number(p.get('variation'))||0,aircraftId:active()?.id||''};try{const r=JSON.parse(localStorage.getItem('pd-route-last')||'null');if(r?.route)return{name:'Latest Route Planner brief',route:r.route,tas:r.tas,burn:r.burn,windDir:r.wd,windSpeed:r.ws,variation:r.variation,aircraftId:active()?.id||''}}catch{}return null}
function fmt(n,d=1){return Number.isFinite(Number(n))?Number(n).toFixed(d):'—'}
async function build(){const f=loadFlight(),host=$('#briefContent');if(!f){host.innerHTML='<div class="pd-empty-state"><h2>No flight selected</h2><p>Save a flight or build a route first, then open its brief.</p><div class="pd-actions"><a class="pd-btn" href="/flights.html">Saved Flights</a><a class="pd-btn secondary" href="/route-planner.html">Route Planner</a></div></div>';$('#briefStatus').textContent='No saved flight or recent route found.';return}const ac=aircraft().find(x=>x.id===f.aircraftId)||active(),parts=String(f.route||'').trim().split(/\s+/).filter(Boolean);if(parts.length<2){host.innerHTML='<div class="pd-empty-state">This flight needs at least a departure and destination.</div>';return}const tas=Number(f.tas||ac?.cruiseTas||ac?.tas||120),burn=Number(f.burn||ac?.fuelBurn||10),windDir=Number(f.windDir??270),windSpeed=Number(f.windSpeed??20),variation=Number(f.variation??0);$('#briefStatus').textContent='Resolving route and loading current endpoint weather…';host.innerHTML='<div class="pd-empty-state">Building current flight brief…</div>';try{const pts=[];for(const p of parts)pts.push(await resolve(p));if(!window.PilotDeskNavlog)throw new Error('Navlog engine did not load.');const b=window.PilotDeskNavlog.build(pts,{tas,burn,windFrom:windDir,windSpeed,variation}),dep=parts[0],dst=parts.at(-1),[dwxR,awxR,dprR,aprR]=await Promise.allSettled([json('/api/weather?station='+encodeURIComponent(dep),9000),json('/api/weather?station='+encodeURIComponent(dst),9000),json('/api/procedures?ident='+encodeURIComponent(dep),15000),json('/api/procedures?ident='+encodeURIComponent(dst),15000)]),dwx=dwxR.status==='fulfilled'?dwxR.value:{station:dep},awx=awxR.status==='fulfilled'?awxR.value:{station:dst},dpr=dprR.status==='fulfilled'?dprR.value:null,apr=aprR.status==='fulfilled'?aprR.value:null,created=f.updatedAt||f.createdAt,procCount=(dpr?.procedures?.length||0)+(apr?.procedures?.length||0);document.title=`${dep} to ${dst} Flight Brief | PilotDesk`;
host.innerHTML=`<section class="pd-brief-hero"><span class="badge">PILOTDESK FLIGHT BRIEF</span><div class="pd-brief-route">${esc(dep)} → ${esc(dst)}</div><h1>${esc(f.name||'Saved flight')}</h1><p>${esc(ac?.name||ac?.type||'No aircraft selected')}${f.date?` · ${esc(f.date)}`:''}</p><div class="pd-brief-grid"><div class="pd-brief-item"><small>Route distance</small><b>${fmt(b.totalDistance,1)} NM</b></div><div class="pd-brief-item"><small>Estimated time</small><b>${fmt(b.totalHours*60,0)} min</b></div><div class="pd-brief-item"><small>Planned fuel</small><b>${fmt(b.totalFuel,1)} gal</b></div><div class="pd-brief-item"><small>Planning TAS</small><b>${fmt(tas,0)} kt</b></div><div class="pd-brief-item"><small>Planning wind</small><b>${fmt(windDir,0)}° / ${fmt(windSpeed,0)} kt</b></div><div class="pd-brief-item"><small>Fuel burn</small><b>${fmt(burn,1)} GPH</b></div><div class="pd-brief-item"><small>Mag variation</small><b>${variation>=0?'+':''}${fmt(variation,1)}°</b></div><div class="pd-brief-item"><small>FAA procedures found</small><b>${procCount||'—'}</b></div></div><div class="pd-actions pd-no-print"><a class="pd-btn" href="/route-planner.html?${f.id?'flight='+encodeURIComponent(f.id):'route='+encodeURIComponent(f.route)}">Open in planner</a><button class="pd-btn secondary" type="button" id="briefPrint">Print brief</button><a class="pd-btn secondary" href="/flights.html">Saved flights</a></div></section><div class="safety-strip"><strong>This is not an official weather briefing or flight release.</strong> Current weather shown here is endpoint context. Check all required weather, NOTAMs, TFRs, forecasts, fuel, airport/runway status, route, airspace, performance, and aircraft information using current official/approved sources before operational use.</div><div class="pd-dashboard-grid"><section class="pd-dashboard-block full"><h2>Route + navlog</h2><div class="pd-raw-report">${esc(parts.join(' → '))}</div><div class="rp-table-wrap" style="margin-top:12px"><table><thead><tr><th>Leg</th><th>TC</th><th>WCA</th><th>MH</th><th>GS</th><th>NM</th><th>ETE</th><th>Fuel</th></tr></thead><tbody>${b.legs.map(x=>`<tr><td>${esc(x.from)} → ${esc(x.to)}</td><td>${fmt(x.course,0)}°</td><td>${fmt(x.wca,1)}°</td><td>${fmt(x.mag,0)}°</td><td>${fmt(x.gs,0)}</td><td>${fmt(x.distance,1)}</td><td>${fmt(x.hours*60,0)}m</td><td>${fmt(x.legFuel,1)}</td></tr>`).join('')}</tbody></table></div></section>${wxCard(dwx,'Departure weather')}${wxCard(awx,'Destination weather')}<section class="pd-dashboard-block full"><h2>FAA terminal procedures</h2><div class="pd-actions"><a class="pd-btn secondary" href="/procedures.html?ident=${encodeURIComponent(dep)}">${esc(dep)} procedures${dpr?.procedures?` (${dpr.procedures.length})`:''}</a><a class="pd-btn secondary" href="/procedures.html?ident=${encodeURIComponent(dst)}">${esc(dst)} procedures${apr?.procedures?` (${apr.procedures.length})`:''}</a></div></section><section class="pd-dashboard-block full"><h2>Official briefing + verification</h2><div class="pd-official-links"><a target="_blank" rel="noopener" href="https://aviationweather.gov/"><b>Aviation Weather Center</b><small>Weather observations, forecasts and advisories</small></a><a target="_blank" rel="noopener" href="https://notams.aim.faa.gov/notamSearch/"><b>FAA NOTAM Search</b><small>Current NOTAM information</small></a><a target="_blank" rel="noopener" href="https://tfr.faa.gov/"><b>FAA TFRs</b><small>Temporary flight restrictions</small></a><a target="_blank" rel="noopener" href="https://www.1800wxbrief.com/Website/"><b>Flight Service</b><small>Official briefing / flight-plan services</small></a></div></section>${f.notes?`<section class="pd-dashboard-block full"><h2>Saved notes</h2><p>${esc(f.notes)}</p></section>`:''}</div><p class="fine">Brief generated ${esc(new Date().toLocaleString())}${created?` · saved flight updated ${esc(new Date(created).toLocaleString())}`:''}.</p>`;$('#briefPrint')?.addEventListener('click',()=>print());$('#briefStatus').textContent='Current flight brief ready.'}catch(e){console.error(e);$('#briefStatus').textContent=e.message||'Flight brief could not be built.';host.innerHTML=`<div class="pd-empty-state"><h2>Brief could not be built</h2><p>${esc(e.message||'Check the route identifiers and try again.')}</p><div class="pd-actions"><a class="pd-btn" href="/route-planner.html?route=${encodeURIComponent(f.route||'')}">Open route planner</a></div></div>`}}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',build,{once:true}):build();
})();
