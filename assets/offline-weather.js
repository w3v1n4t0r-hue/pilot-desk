(()=>{
  if(location.pathname!=='/weather.html')return;
  const init=()=>{const form=document.getElementById('weatherForm');if(!form)return;let pill=document.getElementById('pdOfflinePill');if(!pill){pill=document.createElement('div');pill.id='pdOfflinePill';pill.className='notice';pill.setAttribute('role','status');form.insertAdjacentElement('beforebegin',pill)}const update=()=>{const off=!navigator.onLine;pill.hidden=!off;pill.textContent=off?'Offline — current METAR/TAF cannot be retrieved. PilotDesk does not reuse an older weather report as current. Core math tools remain available with manually verified inputs.':''};update();addEventListener('online',update);addEventListener('offline',update)};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();
})();
